name: DevOps CI Pipeline

on:
  push:
    branches: [ "*" ]
  workflow_dispatch:

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Simple verification test
        run: |
          echo "GitHub Actions pipeline is active!"
          ls -la
